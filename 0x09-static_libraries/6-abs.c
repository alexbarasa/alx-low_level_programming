#include <stdio.h>
/**
 * _abs - Converts any given integer value 
 * to absolute value
 *
 * @n: Integer
 * Return: Absolute value of a given integer
 */
int _abs(int n)
{
	int x;

	if (n < 0)
	{
		x = -x;
		return (x);
	}
	else
		return (x);
}
